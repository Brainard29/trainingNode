var url = "http://myvlogger.io/log";

/*function DisplayLog(log_message) {
    //console.log("Log Message : " + log_message);
    console.log(`Log Message: ${log_message}`);
}
//DisplayLog(`ini perintah pertama logger.js`);

module.exports.TampilkanLog = DisplayLog;

DisplayLog(`ini perintah pertama logger.js`);   */

function DisplayURL() {
    console.log("URL = " + url);
}

//module.exports.TampilkanLog = DisplayURL;
module.exports = DisplayURL;




